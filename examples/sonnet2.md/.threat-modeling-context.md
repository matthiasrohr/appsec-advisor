# Threat Modeling Context

| Field | Value |
|-------|-------|
| Generated | 2026-04-09T10:00:00Z |
| Repository | https://github.com/juice-shop/juice-shop |
| Repo Root | /home/mrohr/juice-shop |
| External Context | not configured |
| Requirements YAML | remote (http://127.0.0.1:8000/appsec-requirements-example.yaml) |
| Known Threats | not found |
| Context Files Read | 4 |

## External Context

No external context endpoint configured. The REST endpoint returned an empty response for the repo URL lookup. Set rest_url in config.json to provide additional context (team ownership, compliance scope, prior findings, architecture notes, or any other relevant information).

## Business Context

docs/business-context.md not present in this repository.

OWASP Juice Shop is an intentionally vulnerable web application used for security training, CTF competitions, and awareness purposes. It is a full-stack JavaScript/TypeScript e-commerce application (Node.js + Angular) with deliberately introduced security vulnerabilities spanning OWASP Top 10 and more. The project is maintained by the OWASP community under MIT license. Version 19.2.1.

## Security Policy

OWASP Juice Shop is an intentionally vulnerable web application, but we still do not want to be surprised by zero day vulnerabilities which are not part of our hacking challenges. Security patches are provided for the latest released minor version (19.2.x). Vulnerabilities not part of hacking challenges should be reported to bjoern.kimminich@owasp.org.

## Architecture Notes

No formal architecture documentation found. Application is a monolithic Node.js/Express backend with Angular 17+ SPA frontend. Uses SQLite (via Sequelize ORM) for data storage. JWT-based authentication with RSA keys stored in encryptionkeys/ directory. REST API exposed at /api/ and /rest/ prefixes.

## API Surface

No OpenAPI/Swagger spec found — surface will be derived from code during reconnaissance.

Key route groups detected:
- /api/* — Sequelize CRUD REST endpoints (auto-generated)
- /rest/* — Custom business logic endpoints
- /oauth2/* — OAuth2 login flow
- /b2b/v2/* — B2B order XML processing

## Deployment Topology

Docker support available (Dockerfile present). docker-compose.test.yml for test environment. Can run standalone on Node.js or containerized. Heroku and Gitpod deployment profiles detected. No Kubernetes manifests found. Production deployment appears to target single-container setup.

## Data Model Summary

Sequelize ORM models found in /models/:
- User (email, password, role, totpSecret, deluxeToken, profileImage, address)
- Product (name, description, price, image)
- Basket / BasketItem
- Order / Delivery
- SecurityQuestion / SecurityAnswer
- Feedback / Complaint / Review
- Address / Card / Wallet
- Challenge (CTF tracking)
- PrivacyRequests / DataExport

## Architecture Decisions (ADRs)

No ADR directory found.

## Environment & Configuration

Environment variables and config keys from config/ directory:
- NODE_ENV
- PORT (default 3000)
- BASE_PATH
- JWT secret embedded in code (not env var — SECURITY ISSUE)
- Database: SQLite file (not configurable via env)
- ctf.key — CTF scoring key
- config/default.yml, config/7ms.yml, various challenge configs

## Recent Changes

No CHANGELOG.md found. Package version: 19.2.1.

## Known Threats (Team-Provided)

No docs/known-threats.yaml found. This is an intentionally vulnerable application — all vulnerabilities are intentional training exercises. However, the threat model will document them as real security findings since this is a security architecture assessment.
