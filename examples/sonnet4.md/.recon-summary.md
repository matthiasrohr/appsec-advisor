# Recon Summary — OWASP Juice Shop

Generated: 2026-04-10T16:47:00Z

## Section 1: Business Context

OWASP Juice Shop v19.2.1 — intentionally vulnerable web application for security training and CTF challenges. Node.js/TypeScript/Express backend serving Angular SPA. SQLite + MarsDB (in-memory NoSQL). Single monolith on port 3000.

## Section 2: Tech Stack

- **Language**: TypeScript/Node.js v20-24
- **Framework**: Express 4.22
- **Frontend**: Angular v16+ (built SPA, served statically)
- **Database**: SQLite via Sequelize v6
- **NoSQL**: MarsDB (marsdb) for reviews/orders
- **Auth**: JWT (RS256) via express-jwt 0.1.3 + jsonwebtoken 0.4.0
- **Template**: Pug (hbs configured, pug used in profile)
- **Container**: Docker (distroless/nodejs24) running as UID 65532

## Section 3: Manifests

- /home/mrohr/juice-shop/package.json
- /home/mrohr/juice-shop/frontend/package.json
- /home/mrohr/juice-shop/Dockerfile
- /home/mrohr/juice-shop/docker-compose.test.yml

## Section 4: Entry Points (Total: 61 routes)

Key unauthenticated routes:
- GET /rest/products/search (SQL injection)
- POST /rest/user/login (SQL injection, no rate limit)
- POST /api/Users (registration)
- GET /ftp/* (directory listing + file download)
- GET /encryptionkeys/* (key files browsable)
- GET /metrics (Prometheus, unauthenticated)
- POST /file-upload (XXE, Zip Slip)
- GET /rest/products/:id/reviews (NoSQL injection)
- GET /api-docs (Swagger UI)
- GET /support/logs/* (log file disclosure)
- GET /redirect (open redirect)
- GET /rest/chatbot/status
- POST /rest/chatbot/respond

Key authenticated routes:
- POST /b2b/v2/orders (RCE via safeEval)
- GET /profile (SSTI via eval)
- PUT /rest/products/:id/reviews (NoSQL injection - Tampering)

## Section 5: CI/CD

GitHub Actions workflows found:
- ci.yml (lint, test, build) — Actions pinned to SHA ✓
- release.yml — Docker build and publish
- codeql-analysis.yml — SAST

## Section 6: Components

1. Express API Server (monolith) — routes, auth, business logic
2. Angular SPA Frontend — client-side rendering, JWT storage
3. SQLite Database — user data, products, orders
4. MarsDB NoSQL — reviews, orders
5. FTP Server — public file hosting (exposed)
6. Prometheus Metrics — telemetry (exposed without auth)

## Section 7: Security Findings

### 7.1 Authentication
- Custom JWT implementation via express-jwt 0.1.3 (very old, pre-dates algorithm enforcement)
- RSA private key HARDCODED in lib/insecurity.ts:27 (Critical)
- JWT stored in localStorage (frontend/src/app/login/login.component.ts:101)
- MD5 used for password hashing (lib/insecurity.ts:43) — not bcrypt/argon2
- No rate limiting on /rest/user/login endpoint

### 7.2 Authorization
- Authorization middleware applied selectively — /api/Products PUT missing auth (server.ts:369 commented out)
- appendUserId does NOT verify user ID matches resource
- /rest/chatbot/respond accessible without auth check visible

### 7.3 Session Management
- JWT tokens stored in both localStorage and cookie (dual storage)
- No CSRF protection (no SameSite, no CSRF tokens)
- OAuth implicit flow used (response_type=token) — deprecated

### 7.4 Input Validation
- SQL injection: routes/search.ts:23 — template literal in raw query
- SQL injection: routes/login.ts:34 — template literal in raw query
- NoSQL injection: routes/showProductReviews.ts:36 — $where clause with user input
- NoSQL injection: routes/updateProductReviews.ts:18 — operator injection on _id

### 7.5 Output Encoding
- Angular [innerHTML] binding with bypassSecurityTrustHtml in search-result (XSS)
- [innerHTML] bindings across 8+ templates without sanitization

### 7.6 Secret Management
- Hardcoded RSA private key: lib/insecurity.ts:27-32 (publicly known key)
- Hardcoded HMAC secret: lib/insecurity.ts:44 ('pa4qacea4VK9t9nGv7yZtwmj')
- Hardcoded cookie secret: server.ts:289 ('kekse')
- Google OAuth client ID in config: default.yml:59

### 7.7 Error Handling
- XXE XML error message leaks file contents in response: routes/fileUpload.ts:87
- Error handler (errorhandler npm) enabled — may expose stack traces

### 7.8 Frontend Security
- DomSanitizer.bypassSecurityTrustHtml used in search-result (XSS)
- Multiple [innerHTML] bindings in templates (admin, about, feedback, search)
- CSP: Dynamic per-profile via profileImage injection — not global CSP header
- CORS: app.use(cors()) — wildcard, all origins allowed

### 7.9 OAuth/OIDC
- Google OAuth using implicit flow (response_type=token) — deprecated in OAuth 2.1
- No PKCE implementation
- Access token handled client-side

### 7.10 Token Storage
- localStorage.setItem('token', ...) — vulnerable to XSS
- No BFF pattern — tokens exposed in browser

### 7.11 Infrastructure
- Runs on port 3000 — no API gateway, no WAF
- FTP directory listing enabled (/ftp)
- Encryption keys directory browsable (/encryptionkeys)
- Log files accessible (/support/logs)
- Prometheus metrics unauthenticated (/metrics)

### 7.12 Secrets in Code
- lib/insecurity.ts:27: RSA private key (MIICXAIBAAKBgQDNwqL****) — CRITICAL
- lib/insecurity.ts:44: HMAC secret ('pa4qa****')
- server.ts:289: Cookie secret ('kekse')
- routes/login.ts:61-66: Multiple hardcoded credential test values

### 7.13 LLM/AI
- No LLM integration detected

### 7.14 Dependency Pinning
- package-lock.json present ✓
- GitHub Actions pinned to SHA ✓
- Docker uses distroless/nodejs24 (no digest pinning — tag only)
- sanitize-html 1.4.2 — very outdated (current: 2.x)
- jsonwebtoken 0.4.0 — very outdated (current: 9.x)
- express-jwt 0.1.3 — very outdated (current: 8.x)

### 7.15 CVE Scanning
- CycloneDX SBOM generated in package.json script (npm run sbom)
- No automated npm audit in CI blocking on High/Critical found

### 7.16 CI/CD Security
- GitHub Actions pinned to SHA (good)
- CodeQL analysis present

### 7.17 Container Security
- Distroless base image (good)
- Runs as UID 65532 non-root (good)
- No digest pinning on base image (tag-only)

### 7.18 HTTP Security Headers
- helmet.noSniff() ✓
- helmet.frameguard() ✓
- NO Content-Security-Policy (global)
- NO HSTS header
- NO Strict-Transport-Security
- CORS: wildcard (app.use(cors())) ❌
- x-powered-by disabled ✓

### 7.19 Frontend Framework
- Angular v16+ detected (frontend/src/app/)
- TypeScript SPA
- No BFF pattern

### 7.20 File Uploads
- POST /file-upload accepts XML (XXE), ZIP (Zip Slip), YAML (bomb)
- file-type library for MIME checking but bypassed by extension check
- Path traversal possible in ZIP extraction

### 7.21 Redirects
- /redirect endpoint with allowlist bypass (substring match)
- isRedirectAllowed uses url.includes() — can bypass by embedding allowed URL

### 7.22 Password Policy
- No password complexity enforced server-side
- MD5 for password storage (no salt visible in code)
- Security questions for password reset (weak reset mechanism)

### 7.23 SSTI/RCE
- routes/userProfile.ts:62 — eval(code) on username pattern match
- routes/b2bOrder.ts:23 — vm.runInContext with safeEval(orderLinesData)
- routes/fileUpload.ts:83 — vm.runInContext('libxml.parseXml...')

### 7.24 SPA/BFF
- No BFF — tokens go directly from login API to localStorage
- Angular guards exist (app.guard.ts) but bypassed by direct API access

## Section 8: Hardcoded Secrets

1. RSA Private Key in lib/insecurity.ts:27 (CRITICAL)
2. HMAC secret 'pa4qa****' in lib/insecurity.ts:44
3. Cookie secret 'kekse' in server.ts:289

## Section 9: Preliminary Components

1. Express API Server (Complex) — auth, routing, business logic, file handling
2. Angular SPA Frontend (Moderate) — client rendering, token storage, OAuth
3. SQLite / Sequelize (Simple) — relational data
4. MarsDB NoSQL (Simple) — reviews and orders
5. File Upload / FTP Server (Simple) — file handling, public access
