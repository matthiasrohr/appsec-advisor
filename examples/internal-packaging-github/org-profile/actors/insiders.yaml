# Example company-specific actor. Bundled into the packaged plugin and
# validated against schemas/actors.schema.yaml by the upstream packager.
actors:
  - id: ACT-E-1
    label: acme-privileged-contractor
    access:
      - internal-network
      - ci-cd-secrets
      - staging-env
    trust_positions:
      - contractor-internal-authority
    capabilities:
      sophistication: medium
      dwell_time: weeks
      surface_reach:
        - local
        - lateral
    motivation: financial
    heatmap_slug: repo-read
