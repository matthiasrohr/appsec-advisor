{
  "permissions": {
    "allow": [
      "Bash(find, git, grep, head, tail, wc, ls, pwd, cat, test, mkdir, curl, echo, date, rm, cp, source, touch, chmod, stat, sort, uniq, tr, cut, diff, tee, xargs)",
      "Read(*)"
    ]
  }
}
