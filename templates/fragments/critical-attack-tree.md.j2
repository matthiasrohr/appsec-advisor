{#- ## Critical Attack Tree — unnumbered section, conditional (critical_count >= 2).
    Inputs: blocks = one-element [{src}] (full `graph LR` tree, composer-built);
    findings[] = composer-derived leaf pointer [{id,title,anchor}].
    The LLM-authored intro/key_takeaway/mitigation_breakpoints/stages fields are
    intentionally NOT rendered — a fixed one-line explanation above the tree and
    a one-line findings pointer below it carry the whole section.
    Dual anchor: legacy #critical-attack-chain preserved for external deep-links (AGENTS.md §5). -#}
<a id="critical-attack-chain"></a>
<a id="critical-attack-tree"></a>
## Critical Attack Tree

The root is the worst-case attacker goal; below it, each capability branch groups the Critical findings that achieve it. Branches feed the goal by OR — any single path suffices.

```mermaid
{{ blocks[0].src }}
```
{% if findings %}
**Findings** (full detail in [§8 Threat Register](#8-threat-register)): {% for f in findings %}{{ f.dot }}[{{ f.id }}]({{ f.anchor }}) {{ f.title }}{% if not loop.last %} · {% endif %}{% endfor %}
{% endif %}
