{#- ## Table of Contents — rendered deterministically from sections-contract.yaml order. -#}
{#- Inputs: entries [{number, title, anchor, children[{title, anchor}]}]. -#}
## Table of Contents

{% for e in entries -%}
{% if e.number %}{{ e.number }}.{% else %}-{% endif %} [{{ e.title }}](#{{ e.anchor }})
{%- for c in e.children %}
   - [{{ c.title }}](#{{ c.anchor }})
{%- endfor %}
{% endfor %}
