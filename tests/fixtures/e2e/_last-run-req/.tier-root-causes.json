{"schema_version": 1, "tier_root_causes": {"server": ["no authentication or session middleware installed", "no authorization or RBAC library in dependencies", "no input validation library (joi, zod, express-validator) present", "no security headers (helmet, CORS, CSRF) configured", "plaintext HTTP only — no TLS termination in application layer"], "data": ["no parameterized query enforcement verified — ORM present but source absent"]}}
