{
  "output": "../plugin/data/appsec-requirements-fallback.yaml",
  "request": {
    "timeout_seconds": 15,
    "auth_header_env": "HARVEST_AUTH_TOKEN",
    "extra_headers": {},
    "use_proxy": false,
    "verify_ssl": false
  },
  "crawl": {
    "requirements_base_url": "",
    "blueprints_base_url": "",
    "max_pages": 100
  },
  "indexing": {
    "_comment": "Controls how much content is stored per source type.",
    "requirements": {
      "mode": "structured",
      "_mode_options": [
        "structured — id, url, text, priority per requirement item (default, compact)",
        "full       — structured + page-level intro/context paragraph(s) per category"
      ]
    },
    "blueprints": {
      "mode": "full",
      "section_max_chars": 5000,
      "_mode_options": [
        "full    — title, summary, topics + all sections with content (default)",
        "summary — title, summary, topics only (no sections, minimal size)"
      ]
    }
  },
  "_comment_overrides": "Per-source mode override: add 'indexing_mode' to any entry in requirements_overrides or blueprints_overrides to override the global mode for that single source.",
  "requirements_overrides": [],
  "blueprints_overrides": []
}
