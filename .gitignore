node_modules/

# ----------------------------------------------------------------------------
# Plugin runtime artifacts (generated when the plugin runs against this repo)
# ----------------------------------------------------------------------------
# All hidden files/dirs under docs/security/ are intermediate/runtime state:
#   .appsec-cache/, .appsec-lock, .appsec-checkpoint,
#   .threat-modeling-context.md, .recon-summary.md, .requirements.yaml,
#   .dep-scan.json, .config-scan.json, .stride-*.json, .threats-merged.json,
#   .triage-flags.json, .architect-review.md,
#   .session-agent-map, .agent-run.log*, .hook-events.log*
docs/security/.*

# Persistent caches (auto-created at runtime)
.cache/
data/appsec-requirements-fallback.yaml

# ----------------------------------------------------------------------------
# OS and editor artifacts
# ----------------------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/settings.json
.idea/

# ----------------------------------------------------------------------------
# Python & venvs
# ----------------------------------------------------------------------------
scripts/venv/
scripts/.venv/
.venv-tests/
.coverage-html/
scripts/harvest-config.json
__pycache__/
*.pyc
.pytest_cache/*

# ----------------------------------------------------------------------------
# Logs
# ----------------------------------------------------------------------------
*.log
npm-debug.log*
